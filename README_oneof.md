This branch is experimenting with two different things:

Using a oneof to collect multiple kind or command message into one
Using an empty message type (SampleAction) to encode a one-shot type command
